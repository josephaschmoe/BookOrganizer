  Subsequent deploys (after code changes)

  firebase deploy
  # or selectively:
  firebase deploy --only hosting
  firebase deploy --only functions

  If the frontend depends on a new or updated callable, deploy hosting and functions together rather than deploying hosting first.
